import 'package:flutter/material.dart';

const visiblePassword = Icon(
  Icons.visibility,
  color: Color(0xff4c5166),
);

const hidePassword = Icon(
  Icons.visibility_off,
  color: Color(0xff4c5166),
);
